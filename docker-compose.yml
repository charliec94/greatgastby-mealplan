services:
  savorly:
    build: .
    container_name: savorly
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      USDA_API_KEY: ${USDA_API_KEY:-}
      RECIPE_API_KEY: ${RECIPE_API_KEY:-}
    restart: unless-stopped
